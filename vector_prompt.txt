This is exactly how you should drive an AI coding agent. If you ask it to build an entire SPLADE ingestion pipeline in one shot, it will hallucinate dependencies, mess up the pgvector syntax for sparse vectors, and tangle your existing logic.

By forcing it into a Contract-First, Iterative Pattern (Skeleton -> Implementation -> Integration -> Test), you stay in the driver's seat as the Principal Engineer, reviewing the architecture at each checkpoint.

System Persona: Act as a Principal Machine Learning Engineer and Enterprise Python Architect. You write modular, highly-optimized code and strictly adhere to Contract-First (OpenSpec) development.

Context: We are upgrading our enterprise RAG ingestion pipeline to a Dual-Encoder architecture. We currently chunk emails and generate dense embeddings. To comply with strict InfoSec rules (no raw text storage allowed in the search database), we are implementing SPLADE (Sparse Vectors) to achieve exact-keyword matching entirely through vector math using pgvector's sparsevec data type.

Goal: Implement the SPLADE embedding generation during our ingestion flow.

Rules of Engagement (Strict):
We will build this iteratively. Do NOT write the entire implementation at once. You must complete one phase, output the requested artifacts for that phase, and explicitly STOP and wait for my approval before proceeding to the next phase.

Phase 1: OpenSpec Contracts & Database Skeleton (Wait for approval)

OpenSpec Definition: Draft a strict OpenAPI/JSON Schema specification defining the input (raw text chunk) and output (a sparse vector format: token indices and their floating-point weights) for the new SPLADE service.

Database Schema: Write the exact PostgreSQL SQL migration command needed to add a sparsevec column to our existing embeddings table to support this new data contract.

Python Skeleton: Create a skeleton Python class SpladeEmbeddingService with empty method signatures and typed hints that perfectly match the OpenSpec you just defined.

Phase 2: Core ML Implementation (Wait for approval)

Implement the logic inside SpladeEmbeddingService.

Load the naver/splade-v3 model using transformers.

Implement the tokenization and max-pooling logic to extract the correct weights for each token dimension.

Format the final output to match pgvector's expected sparse vector string syntax: '{index1:value1, index2:value2}/total_dimensions'. Ensure memory management is handled for CPU/GPU.

Phase 3: Ingestion Integration (Wait for approval)

Integrate SpladeEmbeddingService into our main ingestion loop alongside our existing dense embedding model.

Gate this new pipeline flow strictly behind an environment variable ENABLE_SPLADE_INGESTION (default False).

Phase 4: End-to-End Validation (Wait for approval)

Write a standalone test_splade_ingestion.py script.

It should mock a dummy email chunk, run it through the dual-encoder pipeline, and print the resulting OpenSpec-compliant sparse dictionary to the terminal to validate the math.

Acknowledge these instructions and begin Phase 1.
