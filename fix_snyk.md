In sync_router.py, the _background_embedding_job function has a path traversal vulnerability on db_file_path in the else branch at line 269. Add a _sanitize_file_path() helper that resolves the path against a safe base directory (/app/data/embeddings), strips directory components using Path.name, and raises ValueError if the resolved path escapes the base directory. Apply the sanitizer before the open() call. Do not change any other logic.
